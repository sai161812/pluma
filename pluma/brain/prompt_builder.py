"""pluma.brain.prompt_builder — Route-specific prompt builder (stub).

Spec §10: "Tool schema selection should be route-specific."
Implemented in Phase 9.
"""


class PromptBuilder:
    """Stub: builds route-specific planner prompts. Implemented in Phase 9."""

    def build(self, *args, **kwargs):  # type: ignore[no-untyped-def]
        raise NotImplementedError("PromptBuilder not implemented until Phase 9.")
