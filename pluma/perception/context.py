"""pluma.perception.context — Active window context (stub).

Spec §5.1 Perception Layer: "Active-window identity, UIA tree, targeted
capture, OCR, screen-element references."
Implemented in Phase 7.
Must NOT import pywinauto at module level (boundary rule).
"""


class ActiveWindowContext:
    """Stub: active window identity. Implemented in Phase 7."""

    def get(self):  # type: ignore[no-untyped-def]
        raise NotImplementedError("ActiveWindowContext not implemented until Phase 7.")
