<UserControl
    x:Class="PLUMA.UI.ConflictAlertPopup"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <SolidColorBrush x:Key="ConflictAccentBrush" Color="#B86A6A" />
    </UserControl.Resources>

    <Border Width="440"
            MinHeight="300"
            Background="{ThemeResource AcrylicInAppFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="{ThemeResource OverlayCornerRadius}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Background="{StaticResource ConflictAccentBrush}"
                    CornerRadius="{ThemeResource OverlayCornerRadius}" />

            <Grid Grid.Column="1" Padding="18,16,18,16" RowSpacing="14">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0"
                               Text="PLUMA"
                               Style="{StaticResource CaptionTextBlockStyle}"
                               CharacterSpacing="80" />
                    <TextBlock Grid.Column="1"
                               Text="CONFLICT"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               Style="{StaticResource CaptionTextBlockStyle}" />
                </Grid>

                <StackPanel Grid.Row="1" Spacing="6">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock Text="!"
                                   Foreground="{StaticResource ConflictAccentBrush}"
                                   FontSize="18"
                                   FontWeight="SemiBold"
                                   VerticalAlignment="Center" />
                        <TextBlock Text="Manual resolution required"
                                   FontSize="20"
                                   FontWeight="SemiBold"
                                   TextWrapping="Wrap" />
                    </StackPanel>
                    <TextBlock Text="PLUMA could not safely restore this file automatically."
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               Style="{StaticResource BodyTextBlockStyle}"
                               TextWrapping="Wrap" />
                </StackPanel>

                <StackPanel Grid.Row="2" Spacing="10">
                    <Border Padding="0,0,0,9"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="0,0,0,1">
                        <StackPanel Spacing="3">
                            <TextBlock Text="Current path"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                       Style="{StaticResource CaptionTextBlockStyle}" />
                            <TextBlock Text="Downloads\quarterly_report.pdf"
                                       Style="{StaticResource BodyTextBlockStyle}"
                                       TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>

                    <StackPanel Spacing="3">
                        <TextBlock Text="Conflict path"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   Style="{StaticResource CaptionTextBlockStyle}" />
                        <TextBlock Text="Downloads\quarterly_report.restored.pdf"
                                   Style="{StaticResource BodyTextBlockStyle}"
                                   TextWrapping="Wrap" />
                    </StackPanel>
                </StackPanel>

                <Border Grid.Row="3"
                        Height="1"
                        Background="{ThemeResource CardStrokeColorDefaultBrush}" />

                <StackPanel Grid.Row="4"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            Spacing="4">
                    <Button Content="Open folder"
                            Background="Transparent"
                            BorderThickness="0"
                            Padding="10,6"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    <Button Content="View conflict"
                            Background="Transparent"
                            BorderThickness="0"
                            Padding="10,6"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</UserControl>
